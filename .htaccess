<IfModule mod_rewrite.c>
    RewriteEngine On
    # Tambahkan baris ini lagi, pastikan nama foldernya benar
    RewriteBase /art_commision_ci3/ 
    
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>